import React from 'react';
import Box from '@mui/material/Box';

const Header = () => {
  return (
    <Box
      sx={{
        height: 60,
        background: 'transparent',
      }}>
      {/* Header */}
    </Box>
  );
};

export default Header;
